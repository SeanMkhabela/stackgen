import { Box, Typography } from "@mui/material";

export default function Settings() {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      {/* future: code style preferences, default stack, etc */}
    </Box>
  );
}