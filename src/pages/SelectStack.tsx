import { Box, Typography } from "@mui/material";

export default function SelectStack() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Select your stack</Typography>
      {/* Add dropdowns, toggles, checkboxes here later */}
    </Box>
  );
}